# @silica-protocol/node-models

TypeScript models for the Silica node API surface (JSON-RPC at `/jsonrpc` plus a few REST endpoints).

These types mirror the JSON payloads returned by the node implementation in `protocol/src/api/*`.
